#include <bits/stdc++.h>
using namespace std;

int getMinTime(int n, int d, int p, int k, int di[], int pi[]) {
    if(n == )
}

int main() {
    int n, d, p, k;
    cin >> n >> d >> p >> k;
    int di[n], pi[n];
    for(int i = 0; i < n; i++)
        cin >> di[i];
    for(int i = 0; i < n; i++)
        cin >> pi[i];
}