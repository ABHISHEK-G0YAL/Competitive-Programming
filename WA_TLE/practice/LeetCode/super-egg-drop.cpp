class Solution {
public:
    int superEggDrop(int K, int N) {
        double kk=K;
        double nn=N+1;
        int kth_root=pow(nn,1/kk);
        cout<<K<<" "<<kth_root;
        int sum=0;
        for(int i=1;i<=k;i++) {
            sum+=
        }
    }
};